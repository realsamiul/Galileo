User: fileNode Telemetry.txt
User: 


The Aws end of the setup worked perfect (just had to use AWS_PAGER="" bash setup_proofsheet.sh) . But right as I figured out that the test number they provide cannot reply to messages and that I needed to both publish and configure a proper phone to use as the WhatsApp number (am I correct) I was hit by a baseless restriction on my business portfolio. While that's being reviewed  what can we do. Do we just continue as if WhatsApp is fine and we just ensure the rest is ready to go? Can we ki mock WhatsApp somehow. Or can we migrate this to Facebook messenger 
Model: # You're Right — And Here's What We Do

## First: You're Correct About the Test Number

Yes. Meta's test number can *send* template messages to your registered test recipients, but **it cannot receive replies that trigger your webhook**. To get the full loop (user sends message → webhook fires → Lambda processes → reply sent), you need a registered production number on your WABA. Which requires the business portfolio to be unrestricted.

## The Restriction

Meta's "business portfolio restriction" is their automated risk system. Common triggers:

- New business account + immediate API access attempt
- Account created from a region with high spam volume (Bangladesh qualifies, unfortunately)
- Insufficient "trust signals" (no ad spend history, new page, no website linked)

**Appeal it.** In Business Settings → Business Info → there should be a notification or a "Request Review" option. Keep it factual: "I'm building a document generation SaaS for Bangladeshi small businesses. No spam, no bulk messaging." Usually resolves in 1-7 days. Sometimes 24 hours.

## While You Wait: Three Paths (I Recommend Doing All Three)

---

### Path 1: Migrate the Inbound Layer to Messenger (Live Users Today)

This is the instant-gratification path. **Same Lambda, same Bedrock logic, same document rendering.** Only the message-parsing and message-sending functions change.

Here's why this works immediately:
- Same Meta App — just add "Messenger" product
- No phone number needed — messages come to your Facebook Page
- No business verification needed for basic Messenger bot access
- Free (no per-conversation fee)
- Webhook structure is nearly identical

**What changes in code:**

```python
# ============================================
# MESSENGER ADAPTER — replaces WhatsApp-specific code
# ============================================

def lambda_handler(event, context):
    method = event.get('requestContext', {}).get('http', {}).get('method', 'POST')
    
    if method == 'GET':
        return handle_verification(event)  # SAME as WhatsApp — identical verify logic
    
    try:
        body = json.loads(event.get('body', '{}'))
    except json.JSONDecodeError:
        return {'statusCode': 200}
    
    # DIFFERENCE 1: object type
    if body.get('object') != 'page':
        return {'statusCode': 200}
    
    for entry in body.get('entry', []):
        # DIFFERENCE 2: messaging array instead of changes→value→messages
        for messaging_event in entry.get('messaging', []):
            sender_id = messaging_event['sender']['id']  # Page-scoped user ID
            
            if 'message' in messaging_event:
                message = messaging_event['message']
                
                if message.get('is_echo'):
                    continue  # Skip our own messages
                
                if 'text' in message:
                    user_text = message['text']
                elif 'attachments' in message:
                    # Voice/image — handle later
                    user_text = "[attachment received — text only for now]"
                else:
                    continue
                
                # ===== SAME FROM HERE =====
                history = get_conversation_history(sender_id)
                assistant_response = call_bedrock(user_text, history)
                
                if '[DOCUMENT_READY]' in assistant_response:
                    handle_document_generation_messenger(sender_id, assistant_response)
                else:
                    send_messenger_reply(sender_id, assistant_response)
                
                timestamp = datetime.now(timezone.utc).isoformat()
                save_message(sender_id, timestamp, 'user', user_text)
                save_message(sender_id, timestamp + '_reply', 'assistant', assistant_response)
    
    return {'statusCode': 200}


def send_messenger_reply(recipient_id, text):
    """Send text via Messenger Send API"""
    secrets = get_secrets()
    url = "https://graph.facebook.com/v21.0/me/messages"
    
    # Messenger has a 2000 character limit per message
    if len(text) > 2000:
        chunks = [text[i:i+2000] for i in range(0, len(text), 2000)]
        for chunk in chunks:
            _send_messenger_text(url, secrets['page_access_token'], recipient_id, chunk)
    else:
        _send_messenger_text(url, secrets['page_access_token'], recipient_id, text)


def _send_messenger_text(url, token, recipient_id, text):
    payload = {
        "recipient": {"id": recipient_id},
        "message": {"text": text},
        "messaging_type": "RESPONSE"
    }
    
    http.request(
        'POST', url,
        body=json.dumps(payload),
        headers={
            'Content-Type': 'application/json',
            'Authorization': f'Bearer {token}'
        }
    )


def send_messenger_document(recipient_id, file_bytes, filename):
    """Send a file via Messenger attachment upload"""
    secrets = get_secrets()
    url = "https://graph.facebook.com/v21.0/me/messages"
    
    # Messenger file sending: upload as form data with message
    # Option A: Upload attachment, then send
    upload_url = f"https://graph.facebook.com/v21.0/me/message_attachments"
    
    resp = http.request(
        'POST', upload_url,
        fields={
            'message': json.dumps({
                "attachment": {
                    "type": "file",
                    "payload": {"is_reusable": True}
                }
            }),
            'filedata': (filename, file_bytes, 'application/pdf'),
        },
        headers={
            'Authorization': f'Bearer {secrets["page_access_token"]}'
        }
    )
    
    result = json.loads(resp.data)
    attachment_id = result.get('attachment_id')
    
    if attachment_id:
        # Send message with uploaded attachment
        payload = {
            "recipient": {"id": recipient_id},
            "message": {
                "attachment": {
                    "type": "file",
                    "payload": {"attachment_id": attachment_id}
                }
            },
            "messaging_type": "RESPONSE"
        }
        
        http.request(
            'POST', url,
            body=json.dumps(payload),
            headers={
                'Content-Type': 'application/json',
                'Authorization': f'Bearer {secrets["page_access_token"]}'
            }
        )
    else:
        send_messenger_reply(recipient_id, "⚠️ File upload failed. Trying again...")
```

**Meta Developer Setup for Messenger (5-10 minutes):**

1. In your existing Meta App → "Add Products" → **Messenger** → "Set Up"
2. Under "Access Tokens" → connect your Facebook Page → generate a **Page Access Token**
3. Under "Webhooks" → paste your SAME API Gateway URL, SAME verify token
4. Subscribe to: `messages`, `messaging_postbacks`
5. **That's it.** Message your Facebook Page and your Lambda fires.

**Update your Secrets Manager secret:**
```json
{
  "access_token": "your-whatsapp-token-for-later",
  "phone_number_id": "your-whatsapp-phone-id-for-later",
  "page_access_token": "your-messenger-page-token",
  "verify_token": "same-verify-token-both-use"
}
```

**Key difference:** Messenger doesn't have the 24-hour conversation window restriction for responses. If someone messages your page, you can reply immediately. You DO have a 24-hour window for follow-up messages, but for request→response (which is your entire use case), there's no restriction.

---

### Path 2: Local Mock for End-to-End Testing (Build Confidence Now)

While Meta sorts itself out, you can test every single component except the actual WhatsApp/Messenger send. Here's a dead-simple mock:

**Create a test script (run locally or as a second Lambda):**

```python
import json
import boto3
import requests  # or use urllib3

# Your API Gateway URL
WEBHOOK_URL = "https://abc123xyz.execute-api.us-east-1.amazonaws.com/webhook"

def simulate_whatsapp_message(text, sender="8801712345678"):
    """Send a fake WhatsApp webhook payload to your endpoint"""
    
    payload = {
        "object": "whatsapp_business_account",
        "entry": [{
            "id": "WHATSAPP_BUSINESS_ACCOUNT_ID",
            "changes": [{
                "value": {
                    "messaging_product": "whatsapp",
                    "metadata": {
                        "display_phone_number": "15550000000",
                        "phone_number_id": "TEST_PHONE_NUMBER_ID"
                    },
                    "contacts": [{
                        "profile": {"name": "Test User"},
                        "wa_id": sender
                    }],
                    "messages": [{
                        "from": sender,
                        "id": f"wamid.TEST{hash(text)}",
                        "timestamp": "1699000000",
                        "text": {"body": text},
                        "type": "text"
                    }]
                },
                "field": "messages"
            }]
        }]
    }
    
    response = requests.post(
        WEBHOOK_URL,
        json=payload,
        headers={"Content-Type": "application/json"}
    )
    
    print(f"Status: {response.status_code}")
    print(f"Response: {response.text}")


# Test conversation flow
simulate_whatsapp_message("hi")
simulate_whatsapp_message("invoice banao")
simulate_whatsapp_message("আমার নাম রহিম, Rahim Textiles, Gulshan. Client হলো Karim Traders. 5 pcs shirt @500 taka each, total 2500.")
```

**To capture what your Lambda would send back** (since the actual WhatsApp API call will fail with test credentials), add a debug mode:

```python
# At the top of your Lambda, add:
import os
DEBUG_MODE = os.environ.get('DEBUG_MODE', 'false') == 'true'

# Modify send_reply:
def send_reply(to, text):
    if DEBUG_MODE:
        print(f"[DEBUG] Would send to {to}: {text[:200]}...")
        # Optionally save to DynamoDB for inspection
        save_message(to, datetime.now(timezone.utc).isoformat() + '_debug', 
                    'assistant_debug', text)
        return
    # ... actual send code
```

Set `DEBUG_MODE=true` as a Lambda environment variable. Now you can:
1. Fire mock webhooks at your endpoint
2. Check CloudWatch logs for the full Bedrock response
3. Check DynamoDB for conversation history building correctly
4. Verify document JSON parsing works
5. Verify HTML template rendering works

**This tests everything except the final mile (sending to WhatsApp).**

---

### Path 3: The Smart Play — Build Both, Abstract the Channel

Restructure slightly so your Lambda is channel-agnostic. This takes 15 minutes and means when WhatsApp unlocks, you flip a switch:

```python
# --- Channel Abstraction ---

def parse_inbound(event):
    """
    Parse incoming webhook into a normalized format.
    Returns: (channel, sender_id, message_text, message_type, raw_message)
    """
    body = json.loads(event.get('body', '{}'))
    
    # WhatsApp
    if body.get('object') == 'whatsapp_business_account':
        for entry in body.get('entry', []):
            for change in entry.get('changes', []):
                value = change.get('value', {})
                if 'messages' not in value:
                    continue
                for message in value['messages']:
                    msg_type = message['type']
                    if msg_type == 'text':
                        text = message['text']['body']
                    else:
                        text = None
                    return ('whatsapp', message['from'], text, msg_type, message)
        return None
    
    # Messenger
    elif body.get('object') == 'page':
        for entry in body.get('entry', []):
            for messaging_event in entry.get('messaging', []):
                if messaging_event.get('message', {}).get('is_echo'):
                    continue
                sender_id = messaging_event['sender']['id']
                message = messaging_event.get('message', {})
                if 'text' in message:
                    return ('messenger', sender_id, message['text'], 'text', message)
                elif 'attachments' in message:
                    return ('messenger', sender_id, None, 'attachment', message)
        return None
    
    return None


def send_response(channel, recipient, text):
    """Route outbound message to correct channel"""
    if channel == 'whatsapp':
        send_reply(recipient, text)  # Your existing WhatsApp send
    elif channel == 'messenger':
        send_messenger_reply(recipient, text)


def send_doc_response(channel, recipient, file_bytes, filename):
    """Route document to correct channel"""
    if channel == 'whatsapp':
        send_document(recipient, file_bytes, filename)
    elif channel == 'messenger':
        send_messenger_document(recipient, file_bytes, filename)


# --- Refactored Handler ---
def lambda_handler(event, context):
    method = event.get('requestContext', {}).get('http', {}).get('method', 'POST')
    
    if method == 'GET':
        return handle_verification(event)
    
    parsed = parse_inbound(event)
    if not parsed:
        return {'statusCode': 200}
    
    channel, sender_id, user_text, msg_type, raw = parsed
    
    if user_text is None:
        send_response(channel, sender_id, 
                     "এখন শুধু text message support করি। Voice ও image শীঘ্রই আসছে! 🙏")
        return {'statusCode': 200}
    
    # Everything below is channel-agnostic
    history = get_conversation_history(sender_id)
    assistant_response = call_bedrock(user_text, history)
    
    if '[DOCUMENT_READY]' in assistant_response:
        parts = assistant_response.split('[DOCUMENT_READY]')
        conversational_reply = parts[0].strip()
        json_part = parts[1].strip().replace('```json', '').replace('```', '').strip()
        
        try:
            doc_data = json.loads(json_part)
            html_content = render_document(doc_data['type'], doc_data['data'])
            pdf_bytes = generate_pdf(html_content)
            
            send_response(channel, sender_id, 
                         conversational_reply + "\n\n📄 আপনার document তৈরি হয়েছে!")
            send_doc_response(channel, sender_id, pdf_bytes, 
                            f"{doc_data['type']}.pdf")
        except (json.JSONDecodeError, KeyError) as e:
            send_response(channel, sender_id, 
                         conversational_reply + "\n\n⚠️ Document generation এ সমস্যা হয়েছে। আবার চেষ্টা করুন।")
    else:
        send_response(channel, sender_id, assistant_response)
    
    timestamp = datetime.now(timezone.utc).isoformat()
    save_message(sender_id, timestamp, 'user', user_text)
    save_message(sender_id, timestamp + '_reply', 'assistant', assistant_response)
    
    return {'statusCode': 200}
```

---

## My Actual Recommendation: What to Do Right Now

| Priority | Action | Time |
|---|---|---|
| 1 | Appeal the Meta restriction (do this first, it runs in background) | 5 min |
| 2 | Add Messenger product to your Meta App, get Page Access Token | 10 min |
| 3 | Update your Lambda with the channel-abstraction code above | 20 min |
| 4 | Update Secrets Manager with `page_access_token` | 2 min |
| 5 | Subscribe Messenger webhook to your same API Gateway URL | 5 min |
| 6 | Message your Facebook Page from your personal Facebook — verify the loop works | 5 min |
| 7 | Test document generation end-to-end via Messenger | 10 min |

**Total: ~1 hour and you have a working, live, testable product on Messenger.**

When WhatsApp unlocks (1-7 days typically), you change nothing. The same Lambda handles both. Your users on Messenger keep working. Your users on WhatsApp start working. You've lost zero days.

---

## One More Thing: Messenger Has One Advantage for Testing

Messenger gives you a **persistent conversation** with no 24-hour window rule for responses to user messages. And there's no template message requirement to start. The user messages your page, you reply. That's it. No conversation fees. No template approval. No business verification for basic bot functionality.

For development velocity, it's actually better. You'll be able to iterate on the Bedrock prompts, document templates, and conversation flow 10x faster without the WhatsApp constraints. Then when WhatsApp opens up, you deploy a polished product, not a prototype.

---

Tell me:
1. Do you have a Facebook Page ready to connect (or should I walk you through creating one)?
2. Is your Lambda already deployed from the AWS setup, or do we need to update the code first?
3. Do you want me to give you the complete merged Lambda file (WhatsApp + Messenger + channel abstraction) as one copy-paste-ready deployment?
User: This is the exact script I sh into Aws shell - the only change was an Aws pager item to stop it recursing. #!/bin/bash
set -e

REGION="us-west-2"
PHONE_NUMBER_ID="1213332605203066"
WABA_ID="1756201745793198"
ACCESS_TOKEN=""
VERIFY_TOKEN="proofsheet-verify-2024"
ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)

echo "=========================================="
echo "ProofSheet Setup Starting"
echo "Account: $ACCOUNT_ID"
echo "Region: $REGION"
echo "=========================================="

# 1. Secrets Manager
echo "[1/7] Creating secret..."
aws secretsmanager create-secret \
  --name proofsheet/whatsapp \
  --secret-string "{\"access_token\":\"${ACCESS_TOKEN}\",\"phone_number_id\":\"${PHONE_NUMBER_ID}\",\"verify_token\":\"${VERIFY_TOKEN}\"}" \
  --region $REGION 2>/dev/null || \
aws secretsmanager update-secret \
  --secret-id proofsheet/whatsapp \
  --secret-string "{\"access_token\":\"${ACCESS_TOKEN}\",\"phone_number_id\":\"${PHONE_NUMBER_ID}\",\"verify_token\":\"${VERIFY_TOKEN}\"}" \
  --region $REGION
echo "  Done"

# 2. DynamoDB
echo "[2/7] Creating DynamoDB table..."
aws dynamodb create-table \
  --table-name proofsheet_conversations \
  --attribute-definitions \
    AttributeName=phone_number,AttributeType=S \
    AttributeName=timestamp,AttributeType=S \
  --key-schema \
    AttributeName=phone_number,KeyType=HASH \
    AttributeName=timestamp,KeyType=RANGE \
  --billing-mode PAY_PER_REQUEST \
  --region $REGION 2>/dev/null && echo "  Table created" || echo "  Table already exists"

# 3. S3
echo "[3/7] Creating S3 bucket..."
aws s3 mb s3://proofsheet-docs-${ACCOUNT_ID} --region $REGION 2>/dev/null && echo "  Bucket created" || echo "  Bucket already exists"

# 4. IAM Role
echo "[4/7] Creating IAM role..."
aws iam create-role \
  --role-name proofsheet-lambda-role \
  --assume-role-policy-document '{"Version":"2012-10-17","Statement":[{"Effect":"Allow","Principal":{"Service":"lambda.amazonaws.com"},"Action":"sts:AssumeRole"}]}' 2>/dev/null || true

aws iam attach-role-policy \
  --role-name proofsheet-lambda-role \
  --policy-arn arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole 2>/dev/null || true

aws iam put-role-policy \
  --role-name proofsheet-lambda-role \
  --policy-name proofsheet-permissions \
  --policy-document "{
    \"Version\":\"2012-10-17\",
    \"Statement\":[
      {\"Effect\":\"Allow\",\"Action\":[\"bedrock:InvokeModel\"],\"Resource\":\"*\"},
      {\"Effect\":\"Allow\",\"Action\":[\"secretsmanager:GetSecretValue\"],\"Resource\":\"*\"},
      {\"Effect\":\"Allow\",\"Action\":[\"dynamodb:GetItem\",\"dynamodb:PutItem\",\"dynamodb:UpdateItem\",\"dynamodb:Query\"],\"Resource\":\"arn:aws:dynamodb:${REGION}:${ACCOUNT_ID}:table/proofsheet_*\"},
      {\"Effect\":\"Allow\",\"Action\":[\"s3:PutObject\",\"s3:GetObject\"],\"Resource\":\"arn:aws:s3:::proofsheet-docs-${ACCOUNT_ID}/*\"}
    ]
  }"
echo "  IAM role ready"

# 5. Lambda Function
echo "[5/7] Creating Lambda function..."
cat > /tmp/lambda_function.py << 'LAMBDA'
import json
import boto3
import urllib3
from datetime import datetime, timezone

REGION = 'us-west-2'
bedrock = boto3.client('bedrock-runtime', region_name=REGION)
dynamodb = boto3.resource('dynamodb', region_name=REGION)
secrets_client = boto3.client('secretsmanager', region_name=REGION)
CONVERSATION_TABLE = dynamodb.Table('proofsheet_conversations')
http = urllib3.PoolManager()

_secrets_cache = None

def get_secrets():
    global _secrets_cache
    if _secrets_cache is None:
        resp = secrets_client.get_secret_value(SecretId='proofsheet/whatsapp')
        _secrets_cache = json.loads(resp['SecretString'])
    return _secrets_cache


def lambda_handler(event, context):
    print(f"Event: {json.dumps(event)}")
    method = event.get('requestContext', {}).get('http', {}).get('method', 'POST')

    if method == 'GET':
        return handle_verification(event)

    try:
        body = json.loads(event.get('body', '{}'))
    except json.JSONDecodeError:
        return {'statusCode': 200}

    if body.get('object') != 'whatsapp_business_account':
        return {'statusCode': 200}

    for entry in body.get('entry', []):
        for change in entry.get('changes', []):
            value = change.get('value', {})
            if 'messages' not in value:
                continue
            for message in value['messages']:
                process_message(message)

    return {'statusCode': 200}


def handle_verification(event):
    params = event.get('queryStringParameters', {}) or {}
    mode = params.get('hub.mode', '')
    token = params.get('hub.verify_token', '')
    challenge = params.get('hub.challenge', '')

    print(f"Verify: mode={mode} token={token} challenge={challenge}")

    secrets = get_secrets()

    if mode == 'subscribe' and token == secrets['verify_token']:
        print("Verification SUCCESS")
        return {
            'statusCode': 200,
            'headers': {'Content-Type': 'text/plain'},
            'body': challenge
        }
    print(f"Verification FAILED expected={secrets['verify_token']} got={token}")
    return {'statusCode': 403, 'body': 'Forbidden'}


def process_message(message):
    sender = message['from']
    msg_type = message['type']
    timestamp = datetime.now(timezone.utc).isoformat()

    print(f"Message from {sender} type={msg_type}")

    if msg_type == 'text':
        user_text = message['text']['body']
    elif msg_type == 'interactive':
        interactive = message.get('interactive', {})
        if 'button_reply' in interactive:
            user_text = interactive['button_reply']['title']
        elif 'list_reply' in interactive:
            user_text = interactive['list_reply']['title']
        else:
            user_text = '[interactive]'
    else:
        send_reply(sender, "Right now I only support text messages. Voice and image support coming soon!")
        return

    print(f"User text: {user_text}")

    history = get_conversation_history(sender)
    assistant_response = call_bedrock(user_text, history)

    print(f"Response: {assistant_response[:200]}")

    send_reply(sender, assistant_response)
    save_message(sender, timestamp, 'user', user_text)
    save_message(sender, timestamp + '_r', 'assistant', assistant_response)


SYSTEM_PROMPT = """You are ProofSheet, a document assistant on WhatsApp for users in Bangladesh. You help create professional documents (invoices, letters, leave applications, certificates) from conversational input.

RULES:
- Reply in whatever language the user writes in (Bangla, English, or mixed)
- Be conversational and efficient - ask max 2-3 questions
- When you have enough info, confirm what you will generate
- Be friendly but respect the user's time
- Use emoji sparingly

CAPABILITIES: invoices, receipts, business letters, leave applications, experience certificates, formal applications, agreements.

If someone just says hi, introduce yourself briefly and ask how you can help."""


def call_bedrock(user_text, history):
    messages = []
    for msg in history[-10:]:
        messages.append({"role": msg['role'], "content": msg['content']})
    messages.append({"role": "user", "content": user_text})

    try:
        response = bedrock.invoke_model(
            modelId='anthropic.claude-sonnet-4-20250514',
            body=json.dumps({
                "anthropic_version": "bedrock-2023-05-31",
                "max_tokens": 1024,
                "system": SYSTEM_PROMPT,
                "messages": messages
            })
        )
        result = json.loads(response['body'].read())
        return result['content'][0]['text']
    except Exception as e:
        print(f"Bedrock error: {e}")
        return "Sorry, something went wrong. Please try again."


def get_conversation_history(phone_number):
    try:
        from boto3.dynamodb.conditions import Key
        response = CONVERSATION_TABLE.query(
            KeyConditionExpression=Key('phone_number').eq(phone_number),
            ScanIndexForward=False,
            Limit=20
        )
        items = response.get('Items', [])
        items.reverse()
        return [{'role': item['role'], 'content': item['content']} for item in items]
    except Exception as e:
        print(f"DynamoDB read error: {e}")
        return []


def save_message(phone_number, timestamp, role, content):
    try:
        CONVERSATION_TABLE.put_item(Item={
            'phone_number': phone_number,
            'timestamp': timestamp,
            'role': role,
            'content': content[:5000]
        })
    except Exception as e:
        print(f"DynamoDB write error: {e}")


def send_reply(to, text):
    secrets = get_secrets()
    url = f"https://graph.facebook.com/v21.0/{secrets['phone_number_id']}/messages"

    if len(text) > 4000:
        chunks = [text[i:i+4000] for i in range(0, len(text), 4000)]
        for chunk in chunks:
            _send_text(url, secrets['access_token'], to, chunk)
    else:
        _send_text(url, secrets['access_token'], to, text)


def _send_text(url, token, to, text):
    payload = {
        "messaging_product": "whatsapp",
        "recipient_type": "individual",
        "to": to,
        "type": "text",
        "text": {"preview_url": False, "body": text}
    }
    resp = http.request(
        'POST', url,
        body=json.dumps(payload),
        headers={
            'Content-Type': 'application/json',
            'Authorization': f'Bearer {token}'
        }
    )
    print(f"WhatsApp API: {resp.status} {resp.data.decode()}")
LAMBDA

cd /tmp && zip -j lambda_function.zip lambda_function.py

echo "  Waiting 10s for IAM propagation..."
sleep 10

ROLE_ARN=$(aws iam get-role --role-name proofsheet-lambda-role --query Role.Arn --output text)

aws lambda create-function \
  --function-name proofsheet-webhook \
  --runtime python3.12 \
  --handler lambda_function.lambda_handler \
  --role $ROLE_ARN \
  --zip-file fileb:///tmp/lambda_function.zip \
  --timeout 30 \
  --memory-size 512 \
  --architectures arm64 \
  --region $REGION 2>/dev/null && echo "  Lambda created" || \
(aws lambda update-function-code \
  --function-name proofsheet-webhook \
  --zip-file fileb:///tmp/lambda_function.zip \
  --region $REGION > /dev/null && echo "  Lambda updated")

# 6. API Gateway
echo "[6/7] Creating API Gateway..."
API_ID=$(aws apigatewayv2 create-api \
  --name proofsheet-webhook \
  --protocol-type HTTP \
  --region $REGION \
  --query ApiId --output text)

LAMBDA_ARN=$(aws lambda get-function \
  --function-name proofsheet-webhook \
  --region $REGION \
  --query Configuration.FunctionArn --output text)

INTEGRATION_ID=$(aws apigatewayv2 create-integration \
  --api-id $API_ID \
  --integration-type AWS_PROXY \
  --integration-uri $LAMBDA_ARN \
  --payload-format-version 2.0 \
  --region $REGION \
  --query IntegrationId --output text)

aws apigatewayv2 create-route \
  --api-id $API_ID \
  --route-key "GET /webhook" \
  --target "integrations/$INTEGRATION_ID" \
  --region $REGION > /dev/null

aws apigatewayv2 create-route \
  --api-id $API_ID \
  --route-key "POST /webhook" \
  --target "integrations/$INTEGRATION_ID" \
  --region $REGION > /dev/null

aws apigatewayv2 create-stage \
  --api-id $API_ID \
  --stage-name '$default' \
  --auto-deploy \
  --region $REGION > /dev/null

echo "  API Gateway created"

# 7. Permission
echo "[7/7] Setting permissions..."
aws lambda add-permission \
  --function-name proofsheet-webhook \
  --statement-id apigateway-invoke \
  --action lambda:InvokeFunction \
  --principal apigateway.amazonaws.com \
  --source-arn "arn:aws:execute-api:${REGION}:${ACCOUNT_ID}:${API_ID}/*" \
  --region $REGION 2>/dev/null || true

echo "  Done"

echo ""
echo "=========================================="
echo "SETUP COMPLETE"
echo "=========================================="
echo ""
echo "WEBHOOK URL (paste into Meta dashboard):"
echo "https://${API_ID}.execute-api.${REGION}.amazonaws.com/webhook"
echo ""
echo "VERIFY TOKEN (paste into Meta dashboard):"
echo "${VERIFY_TOKEN}"
echo ""
echo "=========================================="
echo "NEXT STEPS:"
echo "1. Go to Meta Developer Dashboard"
echo "2. WhatsApp > Configuration > Webhook"
echo "3. Callback URL = the webhook URL above"
echo "4. Verify Token = ${VERIFY_TOKEN}"
echo "5. Click Verify and Save"
echo "6. Subscribe to messages webhook field"
echo "7. Reply hi to the test number on WhatsApp"
echo "=========================================="
echo ""
echo "To check logs:"
echo "aws logs tail /aws/lambda/proofsheet-webhook --since 5m --region $REGION"
Model: # Good. Here's Where You Stand and What to Do.

Your AWS infrastructure is deployed and working. The Lambda, API Gateway, DynamoDB, S3, Secrets Manager, IAM — all live. The only thing that doesn't work is the actual WhatsApp message loop because Meta has restricted your business portfolio.

## Immediate Action: Messenger (Live Today)

Your existing Lambda needs minimal changes. Here's the exact plan:

### Step 1: Add Messenger to Your Meta App (5 minutes)

You can do this even with the business portfolio restriction — Messenger basic bot access typically isn't blocked by the same restriction that hits WhatsApp Business API.

1. Go to `developers.facebook.com/apps` → your existing app
2. "Add Products" → **Messenger** → "Set Up"
3. Under "Access Tokens" section → Connect your Facebook Page → Generate **Page Access Token**
4. Under "Webhooks" → paste your **same** API Gateway URL: `https://<your-api-id>.execute-api.us-west-2.amazonaws.com/webhook`
5. Verify token: `proofsheet-verify-2024` (same one)
6. Subscribe to: **messages**

If the restriction blocks this too (possible but less common), I'll give you the mock testing path instead. Try it first.

### Step 2: Update Secrets Manager

Run this in CloudShell:

```bash
AWS_PAGER="" aws secretsmanager update-secret \
  --secret-id proofsheet/whatsapp \
  --secret-string '{
    "access_token":"",
    "phone_number_id":"1213332605203066",
    "verify_token":"proofsheet-verify-2024",
    "page_access_token":"EAAfsLZAnDzj4BRoNUIHofv7M6y7QjE9lvrVXiMmZBnh6ZBDBShvcbZCporbfSD61mC3vTNIjkpyDDaNZAPDQbGOjsYmnnZBe26RSll7VPuD09ZAQOZAtg8xf47sj2hfSFYyoUS4QK2Qc6bkFtRUMu2onYJKITDH9bWr4aiqzZCAhHRK3kkfsi6sIbeo5RNZCuX7tPjzln9tp5foAZDZD"
  }' \
  --region us-west-2
```

Replace `PASTE_YOUR_PAGE_TOKEN_HERE` with the token you get from step 1.

### Step 3: Update Lambda Code

This is the full replacement file. It handles **both** WhatsApp and Messenger through the same endpoint. When WhatsApp unlocks, it works immediately with no changes.

```bash
cat > /tmp/lambda_function.py << 'LAMBDA'
import json
import boto3
import urllib3
from datetime import datetime, timezone

REGION = 'us-west-2'
bedrock = boto3.client('bedrock-runtime', region_name=REGION)
dynamodb = boto3.resource('dynamodb', region_name=REGION)
secrets_client = boto3.client('secretsmanager', region_name=REGION)
CONVERSATION_TABLE = dynamodb.Table('proofsheet_conversations')
http = urllib3.PoolManager()

_secrets_cache = None


def get_secrets():
    global _secrets_cache
    if _secrets_cache is None:
        resp = secrets_client.get_secret_value(SecretId='proofsheet/whatsapp')
        _secrets_cache = json.loads(resp['SecretString'])
    return _secrets_cache


# ==========================================
# MAIN HANDLER
# ==========================================

def lambda_handler(event, context):
    print(f"Event: {json.dumps(event)}")
    method = event.get('requestContext', {}).get('http', {}).get('method', 'POST')

    if method == 'GET':
        return handle_verification(event)

    try:
        body = json.loads(event.get('body', '{}'))
    except json.JSONDecodeError:
        return {'statusCode': 200}

    # Route to correct channel handler
    obj = body.get('object', '')

    if obj == 'whatsapp_business_account':
        handle_whatsapp(body)
    elif obj == 'page':
        handle_messenger(body)
    else:
        print(f"Unknown object type: {obj}")

    return {'statusCode': 200}


def handle_verification(event):
    """Same verification works for both WhatsApp and Messenger webhooks"""
    params = event.get('queryStringParameters', {}) or {}
    mode = params.get('hub.mode', '')
    token = params.get('hub.verify_token', '')
    challenge = params.get('hub.challenge', '')

    print(f"Verify: mode={mode} token={token}")
    secrets = get_secrets()

    if mode == 'subscribe' and token == secrets['verify_token']:
        print("Verification SUCCESS")
        return {
            'statusCode': 200,
            'headers': {'Content-Type': 'text/plain'},
            'body': challenge
        }
    print(f"Verification FAILED expected={secrets['verify_token']} got={token}")
    return {'statusCode': 403, 'body': 'Forbidden'}


# ==========================================
# CHANNEL: WHATSAPP
# ==========================================

def handle_whatsapp(body):
    for entry in body.get('entry', []):
        for change in entry.get('changes', []):
            value = change.get('value', {})
            if 'messages' not in value:
                continue
            for message in value['messages']:
                sender = message['from']
                msg_type = message['type']

                if msg_type == 'text':
                    user_text = message['text']['body']
                elif msg_type == 'interactive':
                    interactive = message.get('interactive', {})
                    if 'button_reply' in interactive:
                        user_text = interactive['button_reply']['title']
                    elif 'list_reply' in interactive:
                        user_text = interactive['list_reply']['title']
                    else:
                        user_text = '[interactive]'
                else:
                    send_whatsapp_reply(sender, "Right now I support text only. Voice & image coming soon! 🙏")
                    return

                process_and_respond(sender, user_text, 'whatsapp')


# ==========================================
# CHANNEL: MESSENGER
# ==========================================

def handle_messenger(body):
    for entry in body.get('entry', []):
        for messaging_event in entry.get('messaging', []):
            # Skip echo of our own messages
            if messaging_event.get('message', {}).get('is_echo'):
                continue

            sender_id = messaging_event['sender']['id']

            if 'message' in messaging_event:
                message = messaging_event['message']

                if 'text' in message:
                    user_text = message['text']
                elif 'attachments' in message:
                    send_messenger_reply(sender_id, "Right now I support text only. Voice & image coming soon! 🙏")
                    return
                else:
                    return

                process_and_respond(sender_id, user_text, 'messenger')

            elif 'postback' in messaging_event:
                # Button taps from persistent menu etc.
                payload = messaging_event['postback'].get('payload', '')
                title = messaging_event['postback'].get('title', payload)
                process_and_respond(sender_id, title, 'messenger')


# ==========================================
# CORE LOGIC (Channel-Agnostic)
# ==========================================

def process_and_respond(sender_id, user_text, channel):
    """All the intelligence lives here — same for both channels"""
    timestamp = datetime.now(timezone.utc).isoformat()

    print(f"[{channel}] From: {sender_id} Text: {user_text}")

    history = get_conversation_history(sender_id)
    assistant_response = call_bedrock(user_text, history)

    print(f"[{channel}] Response: {assistant_response[:200]}")

    # Send response via correct channel
    if channel == 'whatsapp':
        send_whatsapp_reply(sender_id, assistant_response)
    elif channel == 'messenger':
        send_messenger_reply(sender_id, assistant_response)

    # Save conversation
    save_message(sender_id, timestamp, 'user', user_text)
    save_message(sender_id, timestamp + '_r', 'assistant', assistant_response)


# ==========================================
# BEDROCK
# ==========================================

SYSTEM_PROMPT = """You are ProofSheet (প্রুফশীট), a professional document assistant available on messaging platforms, serving users in Bangladesh.

You help create professional documents from conversational input.

RULES:
- Reply in whatever language the user writes in (Bangla, English, or mixed Banglish)
- Be conversational and efficient - ask max 2-3 questions before you have enough info
- Group your questions (don't ask one field at a time)
- Make smart assumptions (today's date, standard formats, BDT currency)
- Be friendly but respect the user's time
- Use emoji sparingly (✅ for confirmations, 📄 for documents)

CAPABILITIES:
- Invoices & Receipts (চালান / রসিদ)
- Business Letters (ব্যবসায়িক চিঠি)
- Leave Applications (ছুটির আবেদন)
- Experience Certificates (অভিজ্ঞতা সনদ)
- Agreements (চুক্তিপত্র)
- Formal Applications
- CVs/Resumes

WHEN READY TO GENERATE:
Tell the user you have enough info and confirm what you'll create. Then output on a new line:

[DOCUMENT_READY]
```json
{"type": "invoice|letter|leave_application|experience_certificate", "data": {...}}
```

DOCUMENT SCHEMAS:

Invoice: {"type":"invoice","data":{"from_name":"","from_address":"","from_phone":"","to_name":"","to_address":"","items":[{"description":"","quantity":0,"rate":0,"amount":0}],"total":0,"date":"","invoice_number":"","notes":""}}

Letter: {"type":"letter","data":{"from_name":"","from_designation":"","from_org":"","to_name":"","to_designation":"","to_org":"","subject":"","body":"","date":""}}

Leave Application: {"type":"leave_application","data":{"to_name":"","to_designation":"","org_name":"","applicant_name":"","applicant_designation":"","leave_type":"","from_date":"","to_date":"","days":0,"reason":"","date":""}}

Experience Certificate: {"type":"experience_certificate","data":{"org_name":"","employee_name":"","designation":"","department":"","joining_date":"","leaving_date":"","responsibilities":"","issue_date":""}}

TONE: Like a competent assistant who respects the user's time. Not robotic, not overly chatty.

If someone just says hi, introduce yourself in 2-3 lines and ask what document they need."""


def call_bedrock(user_text, history):
    messages = []
    for msg in history[-10:]:
        messages.append({"role": msg['role'], "content": msg['content']})
    messages.append({"role": "user", "content": user_text})

    try:
        response = bedrock.invoke_model(
            modelId='anthropic.claude-sonnet-4-20250514',
            body=json.dumps({
                "anthropic_version": "bedrock-2023-05-31",
                "max_tokens": 1024,
                "system": SYSTEM_PROMPT,
                "messages": messages
            })
        )
        result = json.loads(response['body'].read())
        return result['content'][0]['text']
    except Exception as e:
        print(f"Bedrock error: {e}")
        return "Sorry, something went wrong on my end. Please try again in a moment."


# ==========================================
# CONVERSATION MEMORY
# ==========================================

def get_conversation_history(sender_id):
    try:
        from boto3.dynamodb.conditions import Key
        response = CONVERSATION_TABLE.query(
            KeyConditionExpression=Key('phone_number').eq(sender_id),
            ScanIndexForward=False,
            Limit=20
        )
        items = response.get('Items', [])
        items.reverse()
        return [{'role': item['role'], 'content': item['content']} for item in items]
    except Exception as e:
        print(f"DynamoDB read error: {e}")
        return []


def save_message(sender_id, timestamp, role, content):
    try:
        CONVERSATION_TABLE.put_item(Item={
            'phone_number': sender_id,
            'timestamp': timestamp,
            'role': role,
            'content': content[:5000]
        })
    except Exception as e:
        print(f"DynamoDB write error: {e}")


# ==========================================
# WHATSAPP SEND
# ==========================================

def send_whatsapp_reply(to, text):
    secrets = get_secrets()
    url = f"https://graph.facebook.com/v21.0/{secrets['phone_number_id']}/messages"

    if len(text) > 4000:
        chunks = [text[i:i+4000] for i in range(0, len(text), 4000)]
        for chunk in chunks:
            _send_whatsapp_text(url, secrets['access_token'], to, chunk)
    else:
        _send_whatsapp_text(url, secrets['access_token'], to, text)


def _send_whatsapp_text(url, token, to, text):
    payload = {
        "messaging_product": "whatsapp",
        "recipient_type": "individual",
        "to": to,
        "type": "text",
        "text": {"preview_url": False, "body": text}
    }
    resp = http.request(
        'POST', url,
        body=json.dumps(payload),
        headers={
            'Content-Type': 'application/json',
            'Authorization': f'Bearer {token}'
        }
    )
    print(f"WhatsApp send: {resp.status} {resp.data.decode()[:200]}")


# ==========================================
# MESSENGER SEND
# ==========================================

def send_messenger_reply(recipient_id, text):
    secrets = get_secrets()
    token = secrets.get('page_access_token', '')

    if not token:
        print("ERROR: No page_access_token in secrets")
        return

    url = f"https://graph.facebook.com/v21.0/me/messages?access_token={token}"

    # Messenger limit is 2000 chars per message
    if len(text) > 2000:
        chunks = [text[i:i+2000] for i in range(0, len(text), 2000)]
        for chunk in chunks:
            _send_messenger_text(url, recipient_id, chunk)
    else:
        _send_messenger_text(url, recipient_id, text)


def _send_messenger_text(url, recipient_id, text):
    payload = {
        "recipient": {"id": recipient_id},
        "message": {"text": text},
        "messaging_type": "RESPONSE"
    }
    resp = http.request(
        'POST', url,
        body=json.dumps(payload),
        headers={'Content-Type': 'application/json'}
    )
    print(f"Messenger send: {resp.status} {resp.data.decode()[:200]}")


LAMBDA

cd /tmp && zip -j lambda_function.zip lambda_function.py

AWS_PAGER="" aws lambda update-function-code \
  --function-name proofsheet-webhook \
  --zip-file fileb:///tmp/lambda_function.zip \
  --region us-west-2

echo ""
echo "=========================================="
echo "LAMBDA UPDATED - Now supports both WhatsApp and Messenger"
echo "=========================================="
echo ""
echo "NEXT: Add Messenger product to your Meta App"
echo "1. developers.facebook.com/apps → your app → Add Products → Messenger"
echo "2. Generate Page Access Token for your Facebook Page"
echo "3. Update Secrets Manager with the page_access_token"
echo "4. Configure webhook: same URL, same verify token"
echo "5. Subscribe to 'messages' field"
echo "6. Message your Facebook Page — it should reply"
echo "=========================================="
```

### Step 4: Deploy

Run the script above in CloudShell. It only updates the Lambda code — doesn't touch your existing infrastructure.

```bash
AWS_PAGER="" bash update_lambda.sh
```

---

## If Messenger Is Also Blocked

If the business portfolio restriction blocks Messenger too (you'll know immediately when you try to add the product or generate the page token), then we go pure mock testing:

```bash
# Test your webhook directly — simulates a WhatsApp message
curl -X POST "https://YOUR-API-ID.execute-api.us-west-2.amazonaws.com/webhook" \
  -H "Content-Type: application/json" \
  -d '{
    "object": "whatsapp_business_account",
    "entry": [{
      "id": "WABA_ID",
      "changes": [{
        "value": {
          "messaging_product": "whatsapp",
          "metadata": {"display_phone_number": "15550000000", "phone_number_id": "TEST"},
          "contacts": [{"profile": {"name": "Test"}, "wa_id": "8801700000000"}],
          "messages": [{"from": "8801700000000", "id": "wamid.test123", "timestamp": "1699000000", "text": {"body": "hi"}, "type": "text"}]
        },
        "field": "messages"
      }]
    }]
  }'
```

This will:
- ✅ Hit your API Gateway
- ✅ Trigger your Lambda
- ✅ Call Bedrock and get a response
- ✅ Save to DynamoDB
- ❌ Fail on the WhatsApp send (because the token/number is restricted) — but you'll see the full response in CloudWatch logs

Check logs:
```bash
AWS_PAGER="" aws logs tail /aws/lambda/proofsheet-webhook --since 5m --region us-west-2 --format short
```

You'll see the Bedrock response printed. That proves your entire pipeline works except the final delivery.

---

## The Appeal

On `business.facebook.com`:
- Business Settings → Business Info → look for any alert/notification
- Or go to: `business.facebook.com/settings/requests`
- Or: `business.facebook.com/business-support-home`

Write something like:

> "I'm building a B2B SaaS product (ProofSheet) that helps small businesses in Bangladesh generate professional documents (invoices, letters) via WhatsApp. This is a legitimate business tool, not bulk messaging or marketing. I request review of the restriction on my portfolio."

---

## Tell me:
1. Can you add Messenger to your Meta App, or is that also blocked?
2. Want me to give you the curl test commands customized with your actual API Gateway URL so you can verify Bedrock is working right now?
